---
id: LRN-008
type: learning
date: 2026-06-29
tags: [shadcn, calendar, tailwind-v4, css-variables, syntax]
---

# LRN-008 — shadcn Calendar : `size-(--cell-size)` obligatoire en Tailwind v4

| Pattern observé                                                                                                                              | Contexte                                                                                 | Application future                                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `h-[--cell-size]` (syntaxe v3 brackets) ne résout pas les CSS custom properties en Tailwind v4 → cellules écrasées, en-têtes de jours collés | `calendar.tsx` installé via CLI shadcn utilise la syntaxe v3 ; recalibrage manuel requis | Remplacer `h-[--var]`/`w-[--var]` par `size-(--var)` et `min-w-[--var]` par `min-w-(--var)` dans tout composant shadcn migré vers Tailwind v4 |
