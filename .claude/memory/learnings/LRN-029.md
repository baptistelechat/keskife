---
id: LRN-029
type: learning
date: 2026-06-30
tags:
  [
    react-day-picker,
    calendar,
    shadcn,
    custom-modifier,
    daysWithData,
    useMonthActivity,
    performance,
    hook,
  ]
---

# LRN-029 — Pattern `daysWithData` : modifier custom + dot CalendarDayButton + hook par mois

| Pattern observé                                                                                                                                                                                                                                                                                          | Contexte                                                                                    | Application future                                                                                 |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| `daysWithData?: Date[]` prop sur Calendar → `modifiers={{ hasData: daysWithData }}` → DayButton reçoit `(modifiers as Record<string, boolean>).hasData` → dot `absolute bottom-0.5`. Hook `useMonthActivity(initialMonth)` encapsule `useState(month)` + `useEffect(fetch, [y, m])` avec deps primitives | Afficher visuellement les jours ayant des entrées Supabase, fetch optimisé par mois affiché | Ce pattern se généralise à tout "days with X" sur un Calendar shadcn wrappant react-day-picker v10 |

## Références

- [LRN-027](LRN-027.md) — children destructuré pour ajouter le dot
- [LRN-028](LRN-028.md) — getDay() utilisé en parallèle pour weekends
