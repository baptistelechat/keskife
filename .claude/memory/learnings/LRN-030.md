---
id: LRN-030
type: learning
date: 2026-06-30
tags: [supabase, react, order, query, ux, list]
---

# LRN-030 — `ascending: false` en DB, pas dans le composant

| Pattern observé                                                                                                                           | Contexte                                                                                                                                           | Application future                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Inverser l'ordre d'affichage d'une liste → changer `ascending` dans la query Supabase, pas `.reverse()` ou `.toReversed()` côté composant | Dashboard journal Keskife : entrées affichées dans l'ordre chronologique (la plus ancienne en haut), l'utilisateur voulait la plus récente en haut | Toujours corriger à la source (query DB) — un `.reverse()` côté client fonctionne mais c'est un symptôme masqué |
