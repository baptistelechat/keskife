---
id: LRN-010
type: learning
date: 2026-06-29
tags: [shadcn, select, jsx, trigger, ui, react]
---

# LRN-010 — shadcn SelectItem JSX complexe → reproduit dans trigger auto

| Pattern observé                                                                                                              | Contexte                                                                                              | Application future                                                                                          |
| ---------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `SelectItem` accepte du JSX arbitraire (ex: flex + dot coloré) et `SelectValue` dans le trigger le reproduit automatiquement | Ajout d'une pastille colorée (`TAG_COLORS`) devant les labels dans le select Catégorie de `EntryForm` | Tout select avec décorateur visuel (icône, badge, dot) — pas besoin de gérer le rendu du trigger séparément |
