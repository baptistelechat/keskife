---
id: LRN-015
type: learning
date: 2026-06-29
tags: [shadcn, calendar, popover, date-picker, controlled-open, react, ui]
---

# LRN-015 — `<input type="date">` → Popover + Calendar shadcn avec fermeture contrôlée

| Pattern observé                                                                                                                                                                                                                                                                                                                                                                                                                 | Contexte                                                                                                     | Application future                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Pour remplacer un `<input type="date">` natif par le Calendar shadcn : (1) wrapper dans `Popover` avec état `open` contrôlé, (2) `onSelect` ferme le popover via `setOpen(false)`, (3) bouton "Aujourd'hui" en bas du PopoverContent (`border-t p-2`) qui sélectionne `new Date()` et ferme, (4) props Calendar identiques à l'instance de référence : `startMonth` / `endMonth` / `timeZone` / `showWeekNumber` / `fixedWeeks` | Kronik — remplacement des datepickers natifs dans Dashboard (journal) et PeriodSelector (plage custom Stats) | Pattern réutilisable dès qu'un `<input type="date">` doit être remplacé dans ce projet ; aligner toutes les props Calendar sur l'instance de référence EntryForm |
